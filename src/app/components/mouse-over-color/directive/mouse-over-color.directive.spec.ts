import { MouseOverColorDirective } from './mouse-over-color.directive';

describe('MouseOverColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOverColorDirective();
    expect(directive).toBeTruthy();
  });
});
